# @package _global_
# Stage 2A Curriculum Fine-Tuning: Language Recovery & Domain Expansion
# Resumes from Stage 1 fire+smoke checkpoint (AP@50=89.4%)
# Adds: car, person (VisDrone) with aggressive text prompt augmentation

defaults:
  - _self_

paths:
  # Resume exactly from the user's best checkpoint
  stage1_checkpoint: /work/nthujerry123/sam3/experiments/merged_finetune_2xh200/checkpoints/checkpoint_40.pt
  bpe_path: /work/nthujerry123/sam3/sam3/sam3/assets/bpe_simple_vocab_16e6.txt.gz
  experiment_log_dir: /work/nthujerry123/sam3/sam3_v2/experiments/stage2A_language_recovery

categories:
  - id: 0
    name: fire
    supercategory: hazard
  - id: 1
    name: smoke
    supercategory: hazard
  - id: 2
    name: car
    supercategory: vehicle
  - id: 3
    name: person
    supercategory: human

scratch:
  enable_segmentation: False
  resolution: 1008
  consistent_transform: False
  max_ann_per_img: 200

  use_act_checkpoint_vision_backbone: True
  use_act_checkpoint_text_backbone: True
  use_act_checkpoint_encoder: True
  use_act_checkpoint_decoder: True
  use_act_checkpoint_geo_encoder: True

  use_instance_query: true
  d_model: 256
  num_queries: 200
  num_feature_levels: 1
  encoder_dropout: 0.1
  decoder_dropout: 0.1
  boxRPB: "log"
  dac: True
  use_early_fusion: true
  o2m_mask: false

  train_norm_mean: [0.5, 0.5, 0.5]
  train_norm_std: [0.5, 0.5, 0.5]
  val_norm_mean: [0.5, 0.5, 0.5]
  val_norm_std: [0.5, 0.5, 0.5]

  # Batch settings
  train_batch_size: 2
  val_batch_size: 2
  num_train_workers: 4
  num_val_workers: 2
  max_data_epochs: 15         # Stage 2A only needs ~15 epochs to adapt
  target_epoch_size: 1500
  hybrid_repeats: 1
  context_length: 2
  gather_pred_via_filesys: false

  # ⬇ CRITICAL: Stage 2A Curriculum Learning Rates (1/5 of Stage 1)
  # This prevents Catastrophic Forgetting of fire/smoke knowledge
  lr_scale: 1.0
  lr_transformer: ${times:1.6e-4,${scratch.lr_scale}}       # Stage1=8e-4
  lr_vision_backbone: ${times:5e-5,${scratch.lr_scale}}     # Stage1=2.5e-4
  lr_language_backbone: ${times:1e-5,${scratch.lr_scale}}   # Stage1=5e-5
  lrd_vision_backbone: 0.85
  wd: 0.1
  scheduler_timescale: 10
  scheduler_warmup: 5
  scheduler_cooldown: 10

  freeze_text_tower: False
  freeze_image_tower: NoFreeze
  vis_backbone_dp: 0.0

  presence_weight: 20.0
  loss_ce_weight: 20.0
  iabce_pos_weight: 5.0
  iabce_pos_focal: false
  iabce_alpha: 0.25
  instance_query_loss_pad_scale_pos: 1.0
  use_o2m_matcher_on_o2m_aux: false
  scale_by_find_batch_size: True

  tokenizer_ve:
    _target_: sam3.model.tokenizer_ve.SimpleTokenizer
    bpe_path: ${paths.bpe_path}

  pos_embed:
    _target_: sam3.model.position_encoding.PositionEmbeddingSine
    num_pos_feats: ${scratch.d_model}
    normalize: true
    scale: null
    temperature: 10000

  input_geometry_encoder:
    _target_: sam3.model.geometry_encoders.SequenceGeometryEncoder
    pos_enc: ${scratch.pos_embed}
    encode_boxes_as_points: False
    points_direct_project: True
    points_pool: True
    points_pos_enc: True
    boxes_direct_project: True
    boxes_pool: True
    boxes_pos_enc: True
    d_model: ${scratch.d_model}
    num_layers: 3
    use_act_ckpt: ${scratch.use_act_checkpoint_geo_encoder}
    layer:
      _target_: sam3.model.encoder.TransformerEncoderLayer
      activation: "relu"
      d_model: ${scratch.d_model}
      dim_feedforward: 2048
      dropout: ${scratch.encoder_dropout}
      pos_enc_at_attn: false
      pre_norm: True
      pos_enc_at_cross_attn_queries: false
      pos_enc_at_cross_attn_keys: true
      self_attention:
        _target_: sam3.model.attention.MultiheadAttention
        attn_type: Vanilla
        num_heads: 8
        dropout: ${scratch.encoder_dropout}
        embed_dim: ${scratch.d_model}
        batch_first: False
      cross_attention:
        _target_: sam3.model.attention.MultiheadAttention
        attn_type: Vanilla
        num_heads: 8
        dropout: ${scratch.encoder_dropout}
        embed_dim: ${scratch.d_model}
        batch_first: False
    add_cls: true
    add_post_encode_proj: True

  use_presence_eval: True
  original_box_postprocessor:
    _target_: sam3.eval.postprocessors.PostProcessImage
    max_dets_per_img: -1
    use_original_ids: true
    use_original_sizes_box: true
    use_presence: ${scratch.use_presence_eval}

  matcher:
    _target_: sam3.train.matcher.BinaryHungarianMatcherV2
    focal: true
    cost_class: 2.0
    cost_bbox: 5.0
    cost_giou: 2.0
    alpha: 0.25
    gamma: 2
    stable: False

loss:
  _target_: sam3.train.loss.sam3_loss.Sam3LossWrapper
  matcher: ${scratch.matcher}
  o2m_weight: 2.0
  o2m_matcher:
    _target_: sam3.train.matcher.BinaryOneToManyMatcher
    alpha: 0.3
    threshold: 0.4
    topk: 4
  use_o2m_matcher_on_o2m_aux: ${scratch.use_o2m_matcher_on_o2m_aux}
  loss_fns_find:
    - _target_: sam3.train.loss.loss_fns.Boxes
      weight_dict:
        loss_bbox: 5.0
        loss_giou: 2.0
    - _target_: sam3.train.loss.loss_fns.IABCEMdetr
      weak_loss: False
      weight_dict:
        loss_ce: ${scratch.loss_ce_weight}
        presence_loss: ${scratch.presence_weight}
      pos_weight: ${scratch.iabce_pos_weight}
      alpha: ${scratch.iabce_alpha}
      gamma: 2
      use_presence: True
      pos_focal: ${scratch.iabce_pos_focal}
      pad_n_queries: ${scratch.num_queries}
      pad_scale_pos: ${scratch.instance_query_loss_pad_scale_pos}
  loss_fn_semantic_seg: null
  scale_by_find_batch_size: ${scratch.scale_by_find_batch_size}

trainer:
  _target_: sam3.train.trainer.Trainer
  skip_saving_ckpts: false
  empty_gpu_mem_cache_after_eval: True
  skip_first_val: True
  max_epochs: ${scratch.max_data_epochs}
  accelerator: cuda
  seed_value: 42
  val_epoch_freq: 5
  mode: train

  distributed:
    backend: nccl
    find_unused_parameters: True
    gradient_as_bucket_view: True

  loss:
    all: ${loss}
    default:
      _target_: sam3.train.loss.sam3_loss.DummyLoss

  model:
    _target_: sam3.model_builder.build_sam3_image_model
    bpe_path: ${paths.bpe_path}
    device: cpus
    eval_mode: false
    enable_segmentation: ${scratch.enable_segmentation}

  optim:
    amp:
      enabled: True
      amp_dtype: bfloat16
    optimizer:
      _target_: torch.optim.AdamW
    gradient_clip:
      _target_: sam3.train.optim.optimizer.GradientClipper
      max_norm: 0.1
      norm_type: 2
    param_group_modifiers:
      - _target_: sam3.train.optim.optimizer.layer_decay_param_modifier
        _partial_: True
        layer_decay_value: ${scratch.lrd_vision_backbone}
        apply_to: 'backbone.vision_backbone.trunk'
        overrides:
          - pattern: '*pos_embed*'
            value: 1.0
    options:
      lr:
        - scheduler:
            _target_: sam3.train.optim.schedulers.InverseSquareRootParamScheduler
            base_lr: ${scratch.lr_transformer}
            timescale: ${scratch.scheduler_timescale}
            warmup_steps: ${scratch.scheduler_warmup}
            cooldown_steps: ${scratch.scheduler_cooldown}
        - scheduler:
            _target_: sam3.train.optim.schedulers.InverseSquareRootParamScheduler
            base_lr: ${scratch.lr_vision_backbone}
            timescale: ${scratch.scheduler_timescale}
            warmup_steps: ${scratch.scheduler_warmup}
            cooldown_steps: ${scratch.scheduler_cooldown}
          param_names:
            - 'backbone.vision_backbone.*'
        - scheduler:
            _target_: sam3.train.optim.schedulers.InverseSquareRootParamScheduler
            base_lr: ${scratch.lr_language_backbone}
            timescale: ${scratch.scheduler_timescale}
            warmup_steps: ${scratch.scheduler_warmup}
            cooldown_steps: ${scratch.scheduler_cooldown}
          param_names:
            - 'backbone.language_backbone.*'
      weight_decay:
        - scheduler:
            _target_: fvcore.common.param_scheduler.ConstantParamScheduler
            value: ${scratch.wd}
        - scheduler:
            _target_: fvcore.common.param_scheduler.ConstantParamScheduler
            value: 0.0
          param_names:
            - '*bias*'
          module_cls_names: ['torch.nn.LayerNorm']

  checkpoint:
    save_dir: ${launcher.experiment_log_dir}/checkpoints
    save_freq: 5
    resume_from: ${paths.stage1_checkpoint}

  meters:
    val:
      multidomain:
        detection:
          _target_: sam3.eval.coco_writer.PredictionDumper
          iou_type: "bbox"
          dump_dir: ${launcher.experiment_log_dir}/dumps/multidomain
          merge_predictions: True
          postprocessor: ${scratch.original_box_postprocessor}
          gather_pred_via_filesys: ${scratch.gather_pred_via_filesys}
          maxdets: 100
          pred_file_evaluators:
            - _target_: sam3.eval.coco_eval_offline.CocoEvaluatorOfflineWithPredFileEvaluators
              gt_path: /work/nthujerry123/sam3/datasets/visdrone/annotations/val_coco.json
              tide: False
              iou_type: "bbox"
              positive_split: False

  logging:
    tensorboard_writer:
      _target_: sam3.train.utils.logger.make_tensorboard_logger
      log_dir: ${launcher.experiment_log_dir}/tensorboard
      flush_secs: 120
      should_log: True
    wandb_writer: null
    log_dir: ${launcher.experiment_log_dir}/logs
    log_freq: 10

launcher:
  num_nodes: 1
  gpus_per_node: 1
  experiment_log_dir: ${paths.experiment_log_dir}
  multiprocessing_context: forkserver

submitit:
  account: ACD114147
  partition: normal2
  qos: null
  timeout_hour: 48
  use_cluster: False
  cpus_per_task: 16
  port_range: [10000, 65000]
  constraint: null
